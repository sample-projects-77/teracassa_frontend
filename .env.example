# Copy to .env and adjust values. Restart `npm run dev` after changes.
#
# Base URL of the SunHouses backend API (include the /api path).
# Must match how the backend is exposed (scheme, host, port).
VITE_API_BASE_URL=http://localhost:5002/api
